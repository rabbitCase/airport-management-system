//contains staffid of all employees who are assigned an id but are not registered in the system yet, allows for staff registration authentication
let mutableArrayObject = {
    allowed : [6,7,8,9,10]
};

module.exports = { mutableArrayObject };